这不是一个等待玩家提问并回答的 AI 聊天游戏。它是一部由游戏世界持续驱动、由 AI 实时写作、玩家可以随时介入并改变走向的可游玩小说。

1. 世界自主发展，阅读节奏由玩家掌握：默认读完一段再继续，连续阅读由玩家开启。
2. 玩家默认自由输入，而不是默认做选择题。
3. 游戏机制默认隐藏在小说下面，而不是用系统语言打断小说。

# StoryWorld 产品与实现约束

开发游戏交互、叙述、通道或自动推进前，必须阅读 [Narrator Contract](docs/narrator-contract.md)。它是本项目最高优先级的产品体验验收依据：**首先是一部正在实时发生、玩家身处其中的小说，其次才是游戏。** 旧设计中的 Chat Loop 示例不能覆盖此要求。

- 普通回合仅正文；默认不附选项、数值摘要、操作提示、助手提问或保存提示。
- 系统界面由玩家主动打开。世界时钟独立于玩家消息，关键介入点才暂停。
- 世界自主、玩家自主、叙述自主必须分别建模。自动事件不能替玩家做重大决定或消费重要资源。
- Narrator 只表达已提交事实；玩家状态、事件游标和投递状态由规则与事务维护。
- 修改这些行为时运行相关真实 SQLite 测试，至少覆盖自主连续推进、介入暂停、重启、重复扫描、玩家主导权和正文格式。不得只用文案快照证明状态正确。

## 当前开发范围冻结

用户已授权采用剧本 Skills 架构。新的玩法与规则放入 `worlds/<pack>/SKILL.md`、规则文档和登记的计算脚本；通用平台只保留权限、快照、事务、记忆与时钟。实施细节见 [剧本 Skills](docs/story-skills.md)。不能把拼接 Skill 文本冒充官方工具加载，不能把旧版动作 ID 分支包装成新生成引擎。旧剧本保留兼容，不能默认重置或强迁移旧存档。修改此链路需运行 `tests/world_skills_test.py` 与相关渠道/叙事回归，并保留官方运行时及真实模型验收。

按 [生成式文字世界设计](docs/generative-world-design.md) 验证自由意图、实时内容生成和持久后果，暂停装备、技能、宠物、商店、多人、渠道与 API 功能扩展。已有切片只作投递与叙述回归，预写二十段和增加专用交互 ID 不能证明开放性。固定引擎基础约束与世界基本法则，开放内容、语义组合及经验收的规则扩展；不能把合理但未预写的动作等同于越权。保留真实模型端到端及真人阅读验收，mock 和作者正文不能替代。未体验则标记待验证。

涉及功法、资源池、随机能力、克制或冲突裁定时，阅读 [运行时规则补全](docs/runtime-rule-evolution.md)。允许按需生成规则数据，不允许执行模型生成的任意代码、改变基础权限或回溯修改已承诺的交锋；新规则必须验收、版本化并在合法边界生效。当前实现范围见 [动态功法与试法](docs/runtime-rules-play.md)，修改时运行 `tests/runtime_rules_test.py`；真实模型验收脚本仅显式启用时调用 API。
