# 安全说明（Security Policy）

## 报告漏洞

请**不要**在公开 issue 中提交安全漏洞。请发邮件或在 GitHub 上创建 **private security advisory**（Security → Report a vulnerability）。

## 已知威胁模型

本项目是本地部署的 AI 文字冒险平台，自带风险与对策如下，贡献者需知晓：

### 1. LLM Prompt 注入（最重要）
玩家的自由文本会进入 LLM 上下文。恶意玩家可能试图诱导模型：
- 泄露系统提示/世界观包内容 → 对策：模型输出的 `canon_additions` 会写入世界正典，玩家输入只作为"意图摘要"进入导演层，原文不直接入正典
- 控制其他玩家的剧情 → 对策：编剧层只接收"世界公开信息 + 本玩家私有状态"，信息不对称由数据层保证
- 剧本文件本身是外部输入（用户上传），AI 完善后的剧本进入审核队列，**管理员审核通过前不会上架**

### 2. 密钥与配置
- `TELEGRAM_BOT_TOKEN` / `DEEPSEEK_API_KEY` 只放 `.env`（已 gitignore），**不要提交**
- 后台默认账号 `admin/admin123`，部署后必须修改（设置 `ADMIN_USERNAME/ADMIN_PASSWORD`）

### 3. 管理后台
- 后台仅监听 `127.0.0.1`，会话存本地 SQLite（TTL 12h）
- 若需公网访问后台，必须置于反向代理 + 强密码/2FA 之后

### 4. 平台 ToS
- Telegram 官方 API 无此风险；QQ 等社区协议端（OneBot 等）属于灰产地带，建议独立仓库发布，主仓库不内置
- 禁止用本框架做真钱赌博类玩法（违反各平台规则）

### 5. 内容安全
- AI 生成内容存在不可控性，多人世界应有管理员可随时 `/abort` 中止世界（后台"世界管理"提供）
